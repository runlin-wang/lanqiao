#include <stdio.h>

int main() {
    const int N = 25;
    int arr[N][N];
    for (int i = 1; i < N; i+=2) {
        for (int j = 0, k = i; j < i, k > 0; ++j, --k) {
            arr[j][k] = arr[]
        }
    }

    return 0;
}

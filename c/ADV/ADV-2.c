#include <stdio.h>

int main() {
    int n, x, i, j;
    scanf("%d", &n);
    x = (n + 1) / 2;
    
    // 定义一个数组并向其中填充数据 
    int arr[n][n];
    for (i = 0; i < n; i++) {
    	for (j = 0; j < n; j++) {
    		scanf("%d", &arr[i][j]);
    	}
    }
    
    int max = 0;
    for ()
    
    return 0;
}

